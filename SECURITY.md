# Security

Captured agent runs may contain credentials, source code, and personal data. Do not attach real capsules to public issues until the v0.1 redaction gates pass. Before public release, configure private vulnerability reporting for redaction bypass, archive traversal/bomb, replay escape, or unintended network/tool execution.
