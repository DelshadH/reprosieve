Run `python -m scripts.bootstrap` first, before editing anything; it anchors the non-resettable execution clock in the root commit. Then read AGENTS.md, CODEX_TASKS.json, GATE_REGISTRY.json, PROGRESS.json, MANUAL_REQUIRED.json, WORKLOG.md, and every file under docs/. Work autonomously until `python -m scripts.release_gate` passes or every remaining task is blocked only by a valid human-only entry in MANUAL_REQUIRED.json. Do not stop after planning. Use `python -m scripts.next_task`; execute exactly the selected task, append each attempt to WORKLOG.md, apply its cumulative deadline and kill/pivot rule, commit implementation before clean proof generation, create content-addressed evidence, run the registry-owned verifier, update task and owned-gate states together, commit, and loop. Do not ask me for preferences or routine choices. Never mark a task/gate complete from prose, edited status, a constant-pass verifier, or unit tests alone. Do not remove or renumber tasks, gates, required assertions, or contract checks to obtain green. If genuine authentication, ownership, 2FA, legal identity, paid infrastructure, protected settings needs me, add the exact steps to MANUAL_REQUIRED.json and continue all unblocked work.
