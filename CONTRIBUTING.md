# Contributing

Start with a failing reduction/capture/privacy fixture tied to one proof gate. A trace viewer or new framework adapter is out of scope until all core gates pass. Every reducer must preserve referential integrity and include an independent 1-minimality check.
