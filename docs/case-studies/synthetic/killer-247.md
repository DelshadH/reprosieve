# Synthetic case: 247-event mechanical reduction

This fixture is generated by `reprosieve.fixtures.killer_capsule`. It is entirely
synthetic and proves reducer mechanics only.

- Original events: 247.
- Target: one synthetic tool result containing `failure == "needle"`.
- Predicate mode: offline predicate reproduction over materialized values.
- Application code rerun: none.
- Required result: at most 10 events and an independent 1-minimal report.

Run `python scripts/killer_demo.py` to create, reduce, verify, export, and
execute the temporary fixture. The generated artifacts are intentionally not
committed as a real case study.
